<!-- views/index.ejs -->
<!doctype html>
<html>
<head>
    <% include ../partials/head %>
    <title><%= title %></title>
</head>
<body> 
    <div class="mdl-layout mdl-js-layout">
        <%- include("../partials/header") %>

        <main class="mdl-layout__content">
            <div class="mdl-grid container">
                <div class="mdl-cell mdl-cell--7-col">
                    <h2>Scheduly helps you schedule meetings without the back-and-forth emails</h2>
                    <p><i class="fas fa-star"></i>    &nbsp;Get started for free and always free</p>
                    <a href="/signup" class="mdl-button mdl-js-button mdl-button--raised mdl-js-ripple-effect mdl-button--accent">Get Start</a>
                </div>
                <div class="mdl-cell mdl-cell--5-col">
                    <img src="https://assets.calendly.com/assets/landing/homepage/hero-829da70d61c3fc165d69de57bdda58ae45b46fd5436ff022e34ffede540b9404.jpg" max-width="100%">
                </div>
            </div>
            <!--
            <% if (isLoggedIn) { %>
            <div class="row">
                <% if (!products) { %>
                    <p class="text-left">No product found</p>
                <% } else { %>
                    <p class="text-left col-12">Total: <%= products.length %></p>
                
                    <% for(var i=0; i < products.length; i++) {%>
                    <div class="col-3">
                        <img src="<%= products[i].full_image %>" alt="<%= products[i].description%>" class="img-thumbnail rounded">
                        <br></br>
                        <div>
                            <h5><%= products[i].name %></h5>
                            <div>
                                Stock: <%= products[i].stock %>
                            </div>
                            <div>
                                Price: $<%= products[i].price %>
                            </div>
                        </div>
                        <br></br>
                        <div>
                            <form action="/book" method="post">
                                <input type="hidden" value="<%= products[i].id %>" name="product">
                                <input type="submit" class="add-to-cart btn btn-primary" value="Book Now!!"/>
                            </form>
                            
                        </div>
                    </div>
                    <% } %>
                    </div>
                <% } %>
            </main>
            <% } %>
            -->
        </main>
        <% include ../partials/footer %>
    </div>
</body>
</html>